# App port the server listens on
PORT=3000

# Environment: development or production
# Use "production" when deploying (affects logging/static caching)
NODE_ENV=development

# Frontend origin (used for CORS). Example for local dev:
CLIENT_URL=http://localhost:3000

# MongoDB connection string (Atlas example):
# mongodb+srv://<user>:<password>@cluster0.mongodb.net/arliko?retryWrites=true&w=majority
MONGO_URI=your-mongo-db-uri

# Google Gemini API key (kept secret)
GEMINI_API_KEY=your-gemini-api-key

# JWT signing secret (use a long random string)
JWT_SECRET=your-jwt-secret

# Optional: comma-separated DNS servers to override system DNS (use only if needed)
# Example: DNS_SERVERS=8.8.8.8,1.1.1.1
# Leave empty to use system DNS
DNS_SERVERS=

# Notes:
# - On Render/Heroku, set these values in the service's environment variables UI.
